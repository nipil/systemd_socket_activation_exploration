# systemd_socket_activation_exploration
A few programs and script to test with socket the activation feature of system

## Setup

Required packages

    sudo apt install build-essential libsystemd-dev socat

